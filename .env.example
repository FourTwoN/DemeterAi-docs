# DemeterAI v2.0 - Environment Configuration Example
# Copy this file to .env and adjust values for your environment

# =============================================================================
# Logging Configuration
# =============================================================================
# Log level controls verbosity of application logs
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Development: DEBUG (shows all logs including debug statements)
# Staging: INFO (shows info, warnings, errors)
# Production: WARNING (shows only warnings and errors)
LOG_LEVEL=INFO

# =============================================================================
# Database Configuration
# =============================================================================
# Async database connection (asyncpg driver) - used by FastAPI application
# Format: postgresql+asyncpg://username:password@host:port/database_name
DATABASE_URL=postgresql+asyncpg://demeter:password@localhost:5432/demeterai

# Sync database connection (psycopg2 driver) - used by Alembic migrations
# Format: postgresql+psycopg2://username:password@host:port/database_name
# Note: Alembic cannot use async drivers, requires sync psycopg2
DATABASE_URL_SYNC=postgresql+psycopg2://demeter:password@localhost:5432/demeterai

# Connection pool configuration
# Base pool size: Number of persistent connections maintained
# Max overflow: Emergency connections beyond base pool
# Total max connections = DB_POOL_SIZE + DB_MAX_OVERFLOW (30 in this config)
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10

# SQL query logging (set to true for development debugging)
# WARNING: Enabling this in production logs ALL SQL queries (performance impact)
DB_ECHO_SQL=false

# =============================================================================
# Redis Configuration
# =============================================================================
# Redis connection URL (for caching, session storage)
REDIS_URL=redis://localhost:6379/0

# Celery broker and result backend (use Redis for task queue)
# Broker (queue): Database 0
# Result backend (task results): Database 1
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/1

# =============================================================================
# AWS S3 Configuration (Future)
# =============================================================================
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=demeterai-photos

# =============================================================================
# ML Model Configuration (Future)
# =============================================================================
# MODEL_DEVICE=cpu  # Options: cpu, cuda
# SEGMENTATION_MODEL_PATH=models/yolo_segmentation_v11.pt
# DETECTION_MODEL_PATH=models/yolo_detection_v11.pt
# INFERENCE_BATCH_SIZE=8
